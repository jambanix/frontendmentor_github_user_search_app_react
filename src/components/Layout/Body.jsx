import { Search } from "../Search";

export const Body = () => {
  return (
    <div className="flex flex-col gap-4">
      <section>
        <Search />
      </section>

      <section></section>
    </div>
  );
};
